{
  "name": "dmc-trichology-monorepo",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.17.0"
  },
  "private": true,
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "build": "cd website-frontend && npm run build || true",
    "lint": "cd website-frontend && npm run lint || true",
    "test": "echo 'No tests' || exit 0",
    "start": "node backend/server.js"
  }
}
