{
  "name": "dmc-trichology-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "build": "echo 'Root build not required'",
    "start": "node backend/server.js"
  }
}
