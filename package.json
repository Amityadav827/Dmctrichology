{
  "name": "dmc-trichology-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "build": "npm run build --prefix website-frontend",
    "lint": "npm run lint --prefix website-frontend",
    "start": "node backend/server.js"
  }
}
