/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["dmctrichology-1.onrender.com", "res.cloudinary.com", "images.unsplash.com"],
  },
};

export default nextConfig;
